streamlit
pandas
pyyaml
google-cloud-storage # Assuming you still need GCP libs for future steps
google-cloud-bigquery # Assuming you still need GCP libs for future steps
google-cloud-secret-manager # Assuming you still need GCP libs for future steps
textblob
vaderSentiment
pandarallel # Keep for now, but be mindful of threading issues
rich # For RichHandler, though we'll use QueueHandler for Streamlit
tqdm

# Add any other dependencies your project uses